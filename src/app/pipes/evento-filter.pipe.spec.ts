import { EventoFilterPipe } from './evento-filter.pipe';

describe('EventoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
